/**
 * Point type - Represents a 2D coordinate position
 * Used for entity positions, camera positions, and viewport dimensions
 */

/**
 * Point type representing a 2D coordinate
 * @property y - Y coordinate (vertical position)
 * @property x - X coordinate (horizontal position)
 */
export type Point = {
    y: number,
    x: number,
}
